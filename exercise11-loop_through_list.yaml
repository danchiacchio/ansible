---
- name: Exercise 11 - List loop
  hosts: localhost
  gather_facts: false

  vars:
    colors:
      - red
      - green
      - blue
      - pink
      - white

  tasks:
    - name: Print each color
      ansible.builtin.debug:
        msg: "Color: {{ item }}"
      loop: "{{ colors }}"
